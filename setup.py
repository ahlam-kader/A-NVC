from setuptools import setup, find_packages

setup(
    name="ANVC",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "matplotlib"
    ],
    author="Ahlam med abdel kader",
    description="A personal data visualization library combining insight, creativity, and aesthetic design for storytelling",
)
